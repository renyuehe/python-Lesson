from datetime import timedelta

DEFAULT_TIMEOUT = timedelta(days=1,
                            hours=1,
                            seconds=10,
                            minutes=30)

print(DEFAULT_TIMEOUT)